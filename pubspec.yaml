name: website
description: Tomás Sasovsky's personal portfolio — built with Jaspr & Dart
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  jaspr: ^0.23.0
  jaspr_router: ^0.8.2

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.17
  jaspr_builder: ^0.23.0
  lints: ^5.0.0

jaspr:
  mode: static
