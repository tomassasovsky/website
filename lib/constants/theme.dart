import 'package:jaspr/dom.dart';

// Dart-level colour constants — use these inside @css blocks if needed.
const accentColor = Color('#6366f1');
const textColor = Color('#f0f0f0');
const surfaceColor = Color('#1a1a1a');
const borderColor = Color('#272727');
